import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#15f7e6",
      light: "#bb1616",
      contrastText: "#111111",
    },
    sunny: {
      main: "#f30aec",
    },
  },
});

export default theme;
